{
  "$schema": "https://opencode.ai/config.json",
  "default_agent": "plan",
  "agent": {
    "explore": {
      "model": "{{model}}"
    }
  },
  "model": "{{model}}",
  "small_model": "{{model}}"
}
